export enum Embed {
    Void = "VOID_EMBED",
    GetOptions = "GET_OPTIONS_EMBED",
}

export enum Message {
    // # general
    NoLocales = "NO_LOCALES",
    MessageNotFound = "MESSAGE_NOT_FOUND",
    CheckDM = "CHECK_DM",
    CommandDisabled = "COMMAND_DISABLED",
    AdminCommand = "ADMIN_COMMAND",
    CommandNotFound = "COMMAND_NOT_FOUND",
    AdminButton = "ADMIN_BUTTON",
    ButtonNotFound = "BUTTON_NOT_FOUND",
    ModalNotFound = "MODAL_NOT_FOUND",
    AdminModal = "ADMIN_MODAL",
    MenuNotFound = "MENU_NOT_FOUND",
    AdminMenu = "ADMIN_MENU",

    // # ping
    PingSended = "PING_SENDED",
}

export enum Button {
    Void = "VOID_BUTTON"
}

export enum SelectMenu {
    Void = "VOID_MENU"
}

export enum Modal {
    Void = "VOID_MODAL"
}